# Buiy docs

Master index of Buiy's design specs, implementation plans, and reports. Grouped by feature area for discovery and onboarding.

For build/test/dev commands, see `../CLAUDE.md`. This file does not duplicate that content.

## Where to start

**Building a UI with Buiy (users):** the [getting-started guide](guide/getting-started.md) and the
runnable [`examples/`](../examples/) (`hello_button` → `hello_text` → `hello_bsn` → `todomvc` →
the `buiy_gallery` reference app), then the crate rustdoc.

**Working on Buiy internals (contributors / agents)** — reading order for newcomers:

1. [Buiy foundation design](specs/2026-05-07-buiy-foundation/README.md) — the target shape of the library: feature inventory, architectural foundation, sub-spec roadmap. Multi-file folder; start at the README, then read children in the order it lists.
2. [Buiy layout design](specs/2026-05-08-buiy-layout-design/README.md) — the Taffy bridge and the layout/render boundary every other subsystem builds on (multi-file).
3. [Buiy text-rendering design](specs/2026-06-09-buiy-text-rendering-design/README.md) — the cosmic-text engine, glyph producer, and text/editing surface (multi-file).
4. [Buiy verification design](specs/2026-06-15-buiy-verification-design/README.md) — how visual/layout/render correctness is gated (the 5-tier pyramid + golden harness) (multi-file).
5. [MVU-as-core design](specs/2026-06-29-mvu-as-core-design.md) — the primary state interface in `buiy_core`: how widget state is routed through one recordable funnel, replayed byte-identically, and agent-driven.
6. [Docs organization design](specs/2026-05-07-docs-organization-design.md) — how this docs tree is structured.

## Document types

Six document types, each with one job. If a doc does not fit one of these, the type list is wrong, not the doc.

- **Spec** (`specs/`) — *what we are building toward.* Target shape of the code: types, traits, invariants, public API. Long-lived, canonical.
- **Plan** (`plans/`) — *how we get from current code to the target.* Migration steps, file-by-file changes, PR breakdown. Cites the spec it realizes. Goes stale once shipped.
- **Report** (`reports/`) — *findings from a one-shot investigation of our codebase.* Audits, post-mortems. Dated, immutable.
- **Prototype** (`prototypes/`) — *the committed deliverables of a throwaway prototype* (`prototype-first-development`): the build-to-learn journal + retrospective (+ charter/design). Carried over so the *learning* survives; the prototype **code stays unmerged**. Indexed under its feature area's *prototype lineage* line.
- **Prior-art** (`prior-art/<system>/`) — *deep dive on an external system we want to learn from.* Living documents; updated as the external system evolves. One folder per system; the full catalog is the [`prior-art/README.md`](prior-art/README.md) sub-index (this file keeps only the category map).
- **Guide** (`guide/`) — *how to USE Buiy.* Task-ordered, usage-first walkthroughs and recipes for library users (not internals). Long-lived; the [getting-started guide](guide/getting-started.md) is the entry point.

An optional **`Kind:`** header field names recurring *variants within* the spec/plan types — `campaign` (a plan-of-plans), `decision` (an ADR-style note), `values` (a mined data table) — without new types or filename suffixes. `reference-designs/` holds immutable design bundles fed into specs. Full definitions in the [docs-organization design](specs/2026-05-07-docs-organization-design.md).

## Status tags

Specs / plans / reports carry one of:

- `[draft]` — being written, target not yet stable.
- `[active]` — current target / in-flight migration.
- `[landed]` — realized in code; canonical reference.
- `[superseded]` — replaced; entry links to successor.

Prior-art docs carry `[active]` or `[archived]`.

## Catalog

Areas appear here as soon as there is a real doc to slot under them. Each area is a `### ` header with **Specs** and **Plans** subsections; entries are one line each:

```markdown
- [Title](specs/YYYY-MM-DD-name-design.md) — 5–15 word summary. `[draft]`
```

If a doc spans areas, file it under its primary area only. Reference any adjacent topics in the summary.

### Foundation

**Specs**

- [Buiy foundation design](specs/2026-05-07-buiy-foundation/README.md) — feature inventory, architectural foundation, sub-spec roadmap (multi-file). `[draft]`
- [Buiy verification design](specs/2026-06-15-buiy-verification-design/README.md) — realizes the foundation `verification.md` gates #2/#5/#11/#12 as a reftests-first 5-tier pyramid: perceptual metric, structured snapshots, property invariants, reftests + CPU/GPU cross-check, golden persistence + determinism + coverage-by-construction (multi-file). Harness landed (all 7 child tiers built + gated; residue goldens renderer-blocked, tracked in follow-ups.md). Realizes the [visual-bug detection report](reports/2026-06-14-visual-bug-detection-strategy.md). `[active]`
- [Buiy verification follow-ups campaign design](specs/2026-07-01-verification-followups-campaign-design.md) — resolves and re-triages the verification follow-up backlog in [follow-ups.md](plans/follow-ups.md): lands the invariant `PositionKind` (tier-2 positioned/auto-z) axis, hardens the metamorphic tier so `realize` CALLS production `painters_z` (not a parallel copy), extracts the headless quiescence-condition helpers, DRY-collapses the Rust SDF oracle to one canonical `sdf_rounded_rect`, and makes the coverage `enroll::build_app` text-capable to drive a catalog-wide content-presence auto-check. Re-verifies and defers the still-unconsumed reftest/golden machinery (multi-reference aggregation, `golden-prune`, object-store migration, gallery matrix enrollment). `[active]`
- [Parity report-asset test hygiene design](specs/2026-07-01-parity-report-asset-test-hygiene-design.md) — stops six `#[ignore]` GPU render tests from unconditionally `img.save`-ing into committed `docs/reports/parity-*-assets/*.png` (a gratuitous side-effect no assertion consumed; dirtied the tree with adapter-specific bytes). Removes the writes (report illustrations freeze; they are **not** goldens), reasons through the rejected alternatives (`BUIY_BLESS`-gate / redirect / delete), and adds a durable `git diff --exit-code -- docs/reports/` CI guard after the GPU lane. `[landed]`
- [Buiy agent-interface design](specs/2026-06-18-buiy-agent-interface-design/README.md) — one canonical AccessKit semantic tree, N consumers: decomposed `A11yStates`/`A11yRelations` components (no megacomponents), real ECS nesting, a single inbound Action ingress with a live per-instance capability filter, an in-process snapshot/perform contract that doubles as Buiy's headless test driver, and an opt-in Phase-2 `buiy_mcp` transport. Folds in and supersedes the planned `buiy-accessibility-design` slot (multi-file). `[draft]`
- [Buiy WebGPU / browser-rendering support design](specs/2026-06-25-buiy-wasm-browser-support-design.md) — target shape of running Buiy in a browser (`wasm32` → custom Bevy render-graph into a `<canvas>`). **Prototype-validated and re-decided to WebGPU-only for v1** (WebGL2 reach is a deferred milestone). Headline correction from actually running it: buiy's WGSL is **not WebGPU-conformant** — a `fwidth`/`textureSample` uniformity violation the native GPU lane can't see, so nothing painted; **one small native-safe shader fix** is the hard prerequisite, then a styled, texted widget paints (Chrome, real WebGPU adapter). v1 decisions: the shader fix, cfg-gate `arboard` off wasm (+`MemClipboard`), a **lean WebGPU feature set** (drop the 3D crates the meta-feature drags in), a deliberate `Cargo.lock` + `cargo deny`/MSRV update for the backend deps, and a **headless-browser WebGPU CI smoke lane** (the only gate that catches the shader class via real Tint). Deferred to the WebGL2 milestone: band-≤16 repack (B2 — the invalid 17-attr pipeline poisons the whole pass → blank), `Rgba16Float` fallback, the `navigator.gpu` 2-artifact loader; also IME / mobile-keyboard / a11y-sink. Resolves foundation §5; aligns with §2.9. Prototype reference: branch `wasm-proto` (`PROTOTYPE-JOURNAL.md`). Realizes the [feasibility report](reports/2026-06-25-wasm-browser-support-feasibility.md) + [`web-rendering` prior-art](prior-art/web-rendering/). **Its deferred WebGL2-reach + platform-service milestones are superseded by the [browser-reach widening design](specs/2026-06-30-buiy-browser-reach-widening-design.md).** `[active]`
- [Buiy browser-reach widening design (WebGL2 + platform-service)](specs/2026-06-30-buiy-browser-reach-widening-design.md) — graduates the WASM v1 spec's deferred work into a **prototype-validated** target for running Buiy in **any modern browser without experimental flags**. The lever is a **WebGL2 reach build** (unflagged everywhere; WebGPU is still Firefox-default-off / some-Linux-flag-gated / pre-Safari-26-absent) delivered beside the WebGPU build via a `navigator.gpu` two-artifact loader. Headline from actually running it on WebGL2: the full 5-screen gallery **renders correctly and is interactive with zero shader errors** after a **single band-attribute fold** (17→16, the WebGL2 `max_vertex_attributes` cap), and WebGL2 adds only a **small, cargo-deny-clean `Cargo.lock` delta (7 backend crates)**. 9 decisions → 5 staged waves (W1 render reach, W2 mobile hardening [touch + `Rgba16Float` fallback], W3 clipboard, W4 a11y DOM/ARIA overlay [`accesskit_web` doesn't exist], W5 IME DOM bridge [winit#4424 open]). The WebGL2 render dimension becomes **CI-enforced** (software WebGL2 / SwiftShader works headless, unlike software WebGPU). Realizes the [prototype retrospective](reports/2026-06-30-browser-reach-widening-prototype-retrospective.md). `[draft]`

**Plans**

- [Phase 0 foundations](plans/2026-05-07-buiy-phase-0-foundations.md) — workspace, BuiyPlugin, system sets, minimal render/layout/a11y/focus/picking/theme, verification harness skeleton, hello-world Button. `[landed]`
- [Phase 0 closeout](plans/2026-05-08-buiy-phase-0-closeout.md) — render-pipeline draws, AccessKit per-window adapter, `bevy_picking` backend; closes the three substantive deferrals from the Phase 0 self-review. `[landed]`
- [Buiy verification harness](plans/2026-06-15-buiy-verification-impl.md) — Phases 0–4 realizing `buiy-verification-design`: metric+reftests → snapshots+invariants → goldens+determinism → coverage-by-construction + forced-colors live wiring + docs flip. Harness landed; residue goldens renderer-blocked (follow-ups.md). `[landed]`
- [Buiy verification follow-ups campaign](plans/2026-07-01-verification-followups-campaign.md) — realizes `2026-07-01-verification-followups-campaign-design`: drains the actionable verification follow-ups (invariant `PositionKind` axis, `realize`→production `painters_z`, headless quiescence helpers, SDF-oracle Rust DRY, text-capable `enroll::build_app` + catalog-wide content-presence auto-check) and re-triages the deferred remainder with dated notes in follow-ups.md. `[active]`
- [Parity report-asset test hygiene](plans/2026-07-01-parity-report-asset-test-hygiene.md) — realizes `2026-07-01-parity-report-asset-test-hygiene-design`: removes the six committed-PNG writes from the GPU render tests, rewords the five stale doc-comments, and adds the `docs/reports/` CI hygiene guard. RED→GREEN verified on the RX 6700 XT (mtime moved pre-fix, stable post-fix; six writer tests green). `[landed]`
- [Testing-audit resolution campaign](plans/2026-06-18-testing-audit-resolution-campaign.md) — realizes the [testing-infrastructure & coverage audit](reports/2026-06-18-testing-infrastructure-coverage-audit.md): 6 dependency-ordered phases resolving all 43 findings + 2 cleanups (Phase 0 repro/CI, 1 test-helper foundation, 2 coverage gaps, 3 harness/goldens/determinism, 4 quality/docs/absent-dimensions, 5 binary consolidation+nextest, 6 closeout) with 4 decision gates. `[landed]` — all 43 findings + 2 cleanups resolved 2026-06-19; both lanes green; the per-binary link fan-out behind the ~63-min cold baseline (#9) eliminated by 162→7 binaries + nextest (fully-warm full run ~4s).
- [CI security & code-quality hardening](plans/2026-06-21-ci-hardening.md) — from a multi-agent CI audit: commit `Cargo.lock` + `--locked`, SHA-pin every action, verify the Mesa tarball checksum, `permissions`/`concurrency`/`timeout-minutes`, optional-feature compile coverage, an MSRV job, `SECURITY.md`/`CODEOWNERS` + branch protection. Dependabot deliberately declined. Merged in PR #78 (branch protection staged as a manual follow-up). `[landed]`
- [Buiy agent-interface campaign](plans/2026-06-18-buiy-agent-interface-campaign.md) — coordination/sequencing plan for the agent-interface spec: phases P0→P1a-d→P2, each at a fresh-agent review gate; names the BSN/0.19 (accesskit 0.24) dependency gate and the just-in-time per-phase-plan convention. `[active]`
- [Buiy agent-interface P0 — addressing](plans/2026-06-18-buiy-agent-interface-p0-addressing.md) — bite-sized TDD for the version-stable groundwork: `entity_for_node_id` (NodeId→Entity inverse), the snapshot off-by-one fix to the canonical ref, and the 7 new `A11yRole` variants + both stringifiers. No live-tree behavior change. `[active]`
- [Buiy agent-interface P1a — decomposed state surface](plans/2026-06-22-buiy-agent-interface-p1a-states.md) — Wave-1 co-drive plan sequencing `semantic-tree.md`: the outbound decomposed `A11yStates`/`A11yRelations` surface (§3.1 BUILD set, §3.2 DEFER ledger, SC-4). `[landed]`
- [Buiy WebGPU/browser v1](plans/2026-06-26-buiy-webgpu-v1.md) — additive-over-native plan realizing the WebGPU-only v1 of the wasm/browser spec: the one shared WGSL uniformity fix, `cfg(wasm32)` gating, trunk/wasm-bindgen, and the headless-Chrome WebGPU smoke gate. `[landed]`

### Layout

**Specs**

- [Buiy layout design](specs/2026-05-08-buiy-layout-design/README.md) — Taffy bridge, hybrid `Style` builder + decomposed components, anchor positioning, container queries, writing modes, stacking + top layer, transforms + containment (multi-file). `[active]`

**Plans**

- [Buiy layout foundation](plans/2026-05-08-buiy-layout-foundation.md) — Phase 1: 8-step pipeline skeleton, decomposed components for the Phase-0 surface, hybrid `Style` builder, `Button` migration. `[landed]`
- [Buiy layout overflow and scrolling](plans/2026-05-08-buiy-layout-overflow-and-scrolling.md) — Phase 2: `Overflow` / `Scroll` / `ScrollOffset` / `ScrollSnapItem` components, Taffy overflow mapping, scroll-position-doesn't-invalidate invariant. `[landed]`
- [Buiy layout grid](plans/2026-05-09-buiy-layout-grid.md) — Phase 3: `GridParams` + `GridItem`, `TrackSize` / `GridLine` / `GridAreas` value types, `Display::Grid` → Taffy, Subgrid + Masonry warn-once stubs. `[landed]`
- [Buiy layout writing modes](plans/2026-05-10-buiy-layout-writing-modes.md) — Phase 4: `WritingMode` + `WritingModeResolved`, inheritance pass, `LogicalBoxModel` / `LogicalInset` builders, sideways-* warn-once stubs. `[landed]`
- [Buiy layout container queries](plans/2026-05-21-buiy-layout-container-queries.md) — Phase 5: `Container` + `ContainerQuery`, `Length::Cq{w,h,i,b,min,max}`, `cq_activate` / `cq_flip_check` / `cq_flip_rerun` pipeline systems, same-frame re-layout capped at 2× Taffy. `[landed]`
- [Buiy layout anchor positioning](plans/2026-05-21-buiy-layout-anchor-positioning.md) — Phase 6: `Anchor` component + `AnchorNameRegistry` observer-maintained resource, `anchor_resolution` sub-pass 6d, Kahn topological sort with deterministic cycle-edge dropping, per-frame warn dedup. `[landed]`
- [Buiy layout sticky/table/multicol](plans/2026-05-22-buiy-layout-sticky-table-multicol.md) — Phase 7: `sticky_offset` full impl (sub-pass 6a), `table_layout` and `multicol_pack` warn-once stubs (sub-passes 6b/6c), `MultiColumn` component, refactor of `AnchorOverrides` → `PostTaffyPositionOverrides` shared across all four sub-passes. `[landed]`
- [Buiy layout transforms + containment](plans/2026-05-28-buiy-layout-transforms-containment.md) — Phase 8: `UiTransform` + `Translate`/`Rotate`/`Scale` longhands + `compose_transform` (M = T·R·S·M_transform), `transform_composition` sub-pass 6e writing the private `ResolvedTransform` render handoff, `Containment` (contain flags / content-visibility / will-change) with SIZE-containment zeroing. Stacking/top-layer deferred to Phase 9 (6f reads the matrix). `[landed]`
- [Buiy layout stacking + top layer](plans/2026-05-29-buiy-layout-stacking-top-layer.md) — Phase 9: `Stacking` (`ZIndex`/`Isolation`/`TopLayer`), `stacking_context` sub-pass 6f detecting stacking contexts (positioned+z-index, isolation, transform, paint/strict containment, root) and writing the private `StackingContext.painters_z` paint-order handoff, CSS `z_index` ordering, single global top layer + `TopLayerActivation`. Render-side triggers / will-change SC / per-window deferred (§ 7). `[landed]`
- [Buiy layout Position::Fixed](plans/2026-05-29-buiy-layout-position-fixed.md) — Phase 10: `Fixed` resolves against the layout root by re-parenting its Taffy node onto the root in the children-sync pass, `is_fixed_root` predicate, `.fixed()` setter; transformed-ancestor containing block still deferred. `[landed]`
- [Buiy layout multi-column layout](plans/2026-05-29-buiy-layout-multicol-layout.md) — Phase 13: real `multicol_pack` sub-pass 6c — CSS used-value column-count resolver + greedy whole-child packer honoring forced `break-*`; true fragmentation still deferred (tier-E). `[landed]`
- [Buiy layout table layout](plans/2026-05-29-buiy-layout-table-layout.md) — Phase 12: real `table_layout` sub-pass 6b — synthetic-flex column-width resolution + document-order row/row-group grid placement; `colspan`/`rowspan`, caption, `<col>`/`<colgroup>` still deferred (tier-C). `[landed]`
- [Buiy layout content-visibility](plans/2026-05-29-buiy-layout-content-visibility.md) — Phase 11: real `content-visibility: auto` off-screen skip (last-frame `ResolvedLayout` vs margin-expanded viewport + `ContainIntrinsicSize` sentinel size, descendants detached, stateless hysteresis) and `content-visibility: hidden` descendant prune, both in step-1 `sync_styles` + mirrored in `cq_flip_rerun`; blanket deferred-warn retired (repurposed to the Auto-without-hint residual). Auto paint-only skip + `contain-intrinsic-size: auto` remembered-size still deferred. `[landed]`
- [Buiy layout descendant invalidation](plans/2026-05-29-buiy-layout-descendant-invalidation.md) — Phase 14: multi-level container-query geometric cascade — step-8 `cq_descendant_invalidate` walks the descendants of every query container whose `ResolvedLayout` changed this frame into a private `ContainerSizeDirty` set, and step-9 `cq_descendant_rerun` re-translates that set, recomputes Taffy, re-writes `ResolvedLayout`, and re-evaluates container queries so a `Cqw`-sized intermediate `B` re-resolves and a rule-bearing descendant `C` flips its marker the same frame `A` resizes (capped at one re-run per frame; deeper chains settle one level per frame). `[landed]`

### Render

**Specs**

- [Buiy render-pipeline design](specs/2026-06-03-buiy-render-pipeline-design/README.md) — Bevy render-graph integration, render-side component model, clipping, top-layer compositing, effect compositor, atlasing, forced-colors; consumes layout's immutable paint inputs (multi-file). `[active]`
- [Glyph/icon affine transform + transform-origin](specs/2026-07-01-glyph-affine-transform-design.md) — extend the R1 2D affine paint to the coverage (glyph/icon) path + honor `transform-origin` at layout 6e (center pivot), so rotated/scaled text + icons paint off-axis (fixes the disclosure chevron). `[landed]`
- [Nested degraded effect-group forward-composite (case A)](specs/2026-07-01-nested-degraded-forward-composite-design.md) — a nested effect group that degrades under RT-pool pressure while its immediate parent kept its target now forward-composites its (folded) members directly into the parent's `Rgba16Float` target at node step-2a (effect-compositor.md § 2.3), instead of vanishing; also removes the `debug_assert!` that panicked debug builds on any nested degraded group. Corrects a false premise (bounds grow by own direct members only, so a bare-`Opacity` wrapper parent degrades first); defers the degraded-chain + kept-child-under-degraded-parent shapes with rationale + follow-ups. `[landed]`

**Plans**

- [2026-07-01-glyph-affine-transform](plans/2026-07-01-glyph-affine-transform.md) — wave plan for the glyph/icon affine + 6e transform-origin fix (W1 6e pivot + meter, W2 carrier+shader, W3 producers, GPU verify). `[landed]`

- [2026-06-03-buiy-render-r1-component-model](plans/2026-06-03-buiy-render-r1-component-model.md) — render-side decomposed component model; sole definer of the shared render types (`components.rs` + `color.rs`). `[landed]`
- [2026-06-03-buiy-render-r2-clip-rects](plans/2026-06-03-buiy-render-r2-clip-rects.md) — `WriteClipRects` render-prep pass; the `ClipRect`/`AncestorClip` clip-chain components. `[landed]`
- [2026-06-03-buiy-render-r3-transform-bridge](plans/2026-06-03-buiy-render-r3-transform-bridge.md) — Transform/GlobalTransform bridge (clip-and-transform § B): single-writer compose + Update propagation chain. `[landed]`
- [2026-06-03-buiy-render-r4-effect-groups](plans/2026-06-03-buiy-render-r4-effect-groups.md) — `WriteEffectGroups` render-prep pass (effect-compositor.md § 1): `effect_reason_for` former predicate + `EffectGroup`/`EffectReason` marker derivation (opacity<1 / isolate / filter / mix-blend / backdrop-filter), inserted/removed as derived state, scheduled `.after(Animate).before(Picking)`. Per-group geometry/targets deferred to a later Prepare pass. `[landed]`
- [2026-06-03-buiy-render-r5-extract](plans/2026-06-03-buiy-render-r5-extract.md) — per-view `Changed`-gated `extract_buiy_nodes` (architecture § 1.2/§ 3/§ 4) replacing `extract_buiy_draws` at the schedule level: pure `resolve_color_token`/`node_skip_reason`/`extracted_node_for`/`assemble_in_paint_order` mapping (CssHidden + OffscreenAuto skips, `painters_z` forward order) + R5-owned per-view `ExtractedNodes` carrier. Extract rework landed; node-side consumption swap (`ExtractedDraws` → `ExtractedNodes`) pending R6/R8, Phase-0 draw path intact. `[landed]`
- [2026-06-03-buiy-render-r6-prepare-buffers](plans/2026-06-03-buiy-render-r6-prepare-buffers.md) — Prepare phase: single persistent per-frame instance buffer + view uniform; owns `BuiyPrimitiveKind`. `[landed]`
- [2026-06-03-buiy-render-r7-pipelines-shaders](plans/2026-06-03-buiy-render-r7-pipelines-shaders.md) — typed-primitive `SpecializedRenderPipeline`s keyed on target format (`BuiyPrimitiveKey { kind, format }` importing R6's `BuiyPrimitiveKind`): quad `..01` + shadow `..02` SDF WGSL under the `0xB01A_01XX` octets, border folded into the quad SDF, outline is the clip-suppressed quad. Device-free key/descriptor/dedup + `naga` parse tests gate; live `PipelineCache` allocation rides the `#[ignore]` GPU path. `[landed]`
- [2026-06-03-buiy-render-r8-paint-clip-toplayer](plans/2026-06-03-buiy-render-r8-paint-clip-toplayer.md) — R8 render-consumer helpers for clip + top layer (paint-order-and-top-layer.md §1–§6, clip-and-transform.md § A consumption): pure `scissor_rect` (logical `ClipRect` → physical wgpu scissor, `None` ⇒ skip), `clip_for_primitive` (outline clips against `AncestorClip`, else own `ClipRect` — § A.2 / component-model § 7), and `partition_top_layer` (top-layer tail split without re-sort, § 3.1), with `render_paint_order` integrating R5's live `assemble_context_tree` walk + the partition. The forward `painters_z` walk, the hit-test-reverse identity, and the `CssVisibility::Hidden`/`OffscreenAuto` per-entity skip are **reused from R5** (`assemble_in_paint_order`/`node_skip_reason`), not re-implemented (the earlier `flatten_paint_order`/`skips_paint` drafts were dropped as duplicates). Device-free helper tests gate. Tasks 1–7 landed; Task 8 (the GPU consumer — per-entity scissored draw + top-layer composite in `BuiyNode::run`) was superseded by R8b's Option-C node-draw model and delivered on real hardware in the [GPU-verify campaign](plans/2026-06-07-render-gpu-verify-campaign.md). `[superseded]`
- [2026-06-07-buiy-render-r8b-node-draw](plans/2026-06-07-buiy-render-r8b-node-draw.md) — R8b hybrid node-draw model (node-draw-model-design Option C; supersedes R8 Task 8 only): per-instance clip-AABB fragment-discard threaded `ClipRect` → `ExtractedNode.clip` → `PackedInstance.clip_min/clip_max` (stride 36→52, raw `[f32;13]`) → `@location(6)/(7)` vertex attrs → clip-aware quad/shadow WGSL discard, keeping R6's single order-safe `draw` (no grouping/re-sort — `painters_z` preserved verbatim). Top-layer members pack the `[±INFINITY]` full-view sentinel (`clip = None`) so they paint unclipped at the painters_z tail; R9 multi-pass effect-group composite reserved as a documented stub in `BuiyNode::run`. Device-free layout/packing/naga-parse/extract/ordering tests gate; live pipeline registration verified on real hardware in the GPU-verify campaign. `[landed]`
- [2026-06-03-buiy-render-r9-effect-compositor](plans/2026-06-03-buiy-render-r9-effect-compositor.md) — Off-screen effect-group compositor (pillar 6 / effect-compositor.md). The full compositor MATH as pure fns in `render/compositor.rs` — `painted_bounds` (§2.1 union + ink + clip-last), next-pow2-capped-at-view `bucket_extent` (§2.2), `post_order_indices` (§3 bottom-up), `plan_allocation`/`rt_pool_budget` degradation (§2.3, ISOLATION/reserved degrade last), `group_target_descriptor` (fixed `Rgba16Float` linear), `composite_src_over` (linear group-opacity) — plus the structural seams `compositor::register`, the `PreparedEffectGroups` per-view component, and the `BuiyNode::run` step-1/step-2 composite loops. The GPU orchestration (the effect-group extract→prepare data flow, the full `prepare_effect_groups` system, and the live pooled-target rasterize/composite) landed on real hardware in the [GPU-verify campaign](plans/2026-06-07-render-gpu-verify-campaign.md). `[landed]`
- [2026-06-03-buiy-render-r10-atlas](plans/2026-06-03-buiy-render-r10-atlas.md) — the shared `BuiyAtlas` render resource (guillotiere allocation, LRU eviction, page-budget pressure, warmup, pooling), the glyph-alpha + icon primitive shapes, and the reserved gradient/mask entry kinds. Realizes atlas-and-text-seam.md. `[landed]`
- [2026-06-03-buiy-render-r11-color-forced-colors-verify](plans/2026-06-03-buiy-render-r11-color-forced-colors-verify.md) — color + forced-colors + verification (color-and-forced-colors.md). Consolidates the token resolver to the single canonical `render::color::resolve_token` (R1's mod.rs + R5's extract copies removed; missing-token `warn!` preserved in `resolve_named`; `SystemColor(kw)` resolved through the active theme); `SystemColorKeyword::{token, ALL}`; the forced-colors stub theme + main-world `Theme`-swap system (`BuiySet::Style`, before extract) + `Theme::is_changed()` re-resolve; the WCAG contrast helper (`buiy_verify` delegates to `render::color::contrast_ratio`, no dup — gate #9); and the gate-#11 static analyzers (token-flow + no-shadow-only-affordance) over a `CatalogPaint` seam. Headless-gated; the gate-#2 golden harness is GPU `#[ignore]`. **Final render-pipeline phase.** `[landed]`
- [2026-06-07-render-gpu-verify-campaign](plans/2026-06-07-render-gpu-verify-campaign.md) — verified the R6–R11 GPU path on real hardware; built the deferred atlas-glyph + effect-compositor + node-draw GPU orchestration (R8b/R9 GPU consumers); added the `--ignored` GPU CI lane; fixed 4 real GPU bugs. `[landed]`
- [Nested degraded forward-composite (case A)](plans/2026-07-01-nested-degraded-forward-composite.md) — realizes `2026-07-01-nested-degraded-forward-composite-design`: generalizes the degraded-group fold (rename `fold_degraded_groups`), adds the node step-2a `(child=None, parent=Some)` injection into the parent target, a headless `plan_allocation` pin + flipped fold unit test + a case-A GPU positive test. RED→GREEN on RX 6700 XT. `[landed]`
- [Cross-phase follow-ups](plans/follow-ups.md) — rolling cross-phase deferral backlog (layout / render / text / verification items, with `LANDED` flips); intentionally undated. `[active]`
- [Buiy follow-ups drain](plans/2026-06-18-followups-drain.md) — campaign plan to drain every actionable open follow-up in `follow-ups.md` (layout / render / text-editing tracks by file-locality), updating each spec + backlog entry as it lands and re-classifying the blocked/superseded/deferred ones. `[landed]`

### Text

**Specs**

- [Buiy text-rendering design](specs/2026-06-09-buiy-text-rendering-design/README.md) — cosmic-text 0.19 engine ownership, Taffy measure seam, glyph producer into the GPU-verified atlas, font assets, decoration/selection/caret painting, editing + IME (multi-file). The whole text subsystem is implemented: the rendering surface (text campaign T1–T9) and the editing/IME surface (the `buiy-text-editing` campaign E1–E6). `[landed]`

**Plans**

- [Buiy text campaign](plans/2026-06-09-buiy-text-campaign.md) — T1–T9 phasing realizing the text-rendering spec; editing/IME implementation deferred to a successor campaign. `[landed]`
- [Buiy text T1 — engine foundation](plans/2026-06-09-buiy-text-t1-engine-foundation.md) — cosmic-text 0.19 dep (default features only; `shape-run-cache` OFF), `buiy_core::text` module, embedded Fira Sans latin subset + `tools/fonts/subset_default_font.sh` provenance script (OFL-1.1, default-on `default_font` feature), `SharedFontSystem` + deterministic `BuiyFallback` + `BuiyTextPlugin`, render-world `BuiySwashCache` (uncached-only), opt-in background system-font scan + `FontsGeneration`. `[landed]`
- [Buiy text T2 — Text component + Buffer lifecycle](plans/2026-06-10-buiy-text-t2-component-buffer-lifecycle.md) — the retained `TextBuffer` (0.19 lazy setters, `Shaping::Advanced` pin, bypass-change-detection discipline), `Text` + `FontFamily`/`FontSize`/`FontWeight` + `TextStyleDefaults` plugin defaults, `TextColor` (`CurrentColor` default), the `ComputedTextLayout` output type, the white-space collapse pre-pass (§ 5.2 value table), `BuiyLayoutStep::TextSync` (trigger union incl. the `FontsGeneration` sweep, intrinsics invalidation, Taffy `mark_dirty`, `Text`-removal cleanup). Measure/`TextCommit` are T3. `[landed]`
- [Buiy text T3 — Measure + wrap/align](plans/2026-06-10-buiy-text-t3-measure-wrap-align.md) — the Taffy measure seam: `TaffyTree<()> → TaffyTree<Entity>` with edge-triggered context registration, `compute_roots_with_text_measure` at all three compute sites (lock-scoped, cq-re-entrant, ≤2× ceiling preserved), the measure protocol (cached min/max-content intrinsics, definite-width relayout fold, ceil policy), intrinsic keywords on text leaves, the `LineHeight`/`WhiteSpace`/`TextWrap`/`TextAlign` carriers + value tables (balance/pretty/stable + justify-all warn-once degrades), `BuiyLayoutStep::TextCommit` (reshape at final content-box, align at commit, `ResolvedBaseline` + idempotent `ComputedTextLayout`), `TextMeasureCallCount`/`TextCommitReshapeCount` steady-state instruments. First pixels are T4. `[landed]`
- [Buiy text T4 — First pixels](plans/2026-06-10-buiy-text-t4-first-pixels.md) — the glyph producer: `extract_buiy_glyphs` in `ExtractSchedule` `.after(maintain_atlas)` (`physical()` 4-bin quantization, 19 B structured `AtlasKey` + `FontKeyInterner`, `get_image_uncached`-on-miss = lock site #3, § 5.2 rect math + straight-alpha `TextColor` + self-inclusive clip), the § 6.2 retain-with-probe damage gate (cached-`f32` scale compare, never `Changed<Window>`) + the un-gated `ResidentTextKeys` touch pass, `GlyphMetaCache` bearings, color-emoji skip+warn (IconInstance seam named), real-entity GPU fixtures replacing atlas_gpu.rs's test-as-producer fill, `wait_for_fonts`/`warm_atlas` realized (`fonts_ready`), the `hello_text` example. Cross-layer quad/glyph interleave stays a render-spec dependency (z-artifacts on layered fixtures are expected until it lands). `[landed]`
- [Buiy text T5 — Fonts, fallback, and BiDi](plans/2026-06-10-buiy-text-t5-fonts-fallback-bidi.md) — `BuiyFont` asset + `BuiyFontLoader` (sfnt invariant = the named woff2 seam; Modified = remove+re-add), `FontRegistry` (strong handles, declared family names, in-place `load_font_source` add, rebuild-on-remove via `into_locale_and_db` — surviving fontdb IDs verified stable in-lineage; `FontDbLineage` + monotonic `FontKeyInterner` reseat close the fresh-db AtlasKey-aliasing hazard), the lock-free `FontStack` resolver (`FontMatchIndex` db snapshot + skrifa-charmap coverage, fontdb `Query` matching, coverage span-splitting via `set_rich_text`, `unicode-range`, `FontFallbackIter` as the implicit last resort), `font-display` Swap/Block (zero-alpha + 3 s timeout), `TextDirection` strong-mark prepend (§ 5.4), per-script OFL fixture subsets + the multi-script shaping-snapshot corpus, GPU multi-script golden + rebuild-storm bound. `[landed]`
- [Buiy text T6 — Decoration painting](plans/2026-06-10-buiy-text-t6-decoration-painting.md) — `TextDecorations` (`DecorationLines` bitflags + `DecorationLineStyle` Solid/Double, dotted/dashed/wavy degrade warn-once) lowered to `Attrs.text_decoration` at TextSync (line bits only; the `-color` token resolves at extract), the pure `render_decoration` mirror (f32 logical px, the § 3.3 physical-px min-thickness floor + y-snap, upstream's int-px `.max(1).ceil()` deliberately not copied), underline/overline → the entity-keyed `ExtractedTextQuads` carrier spliced into the existing quad buffer by the § 4.6 fresh-node-list walk (group adoption, contiguity by construction, triple gate `nodes||groups||text_quads`), line-through → 1×1 warmup-pinned `CoverageR8` solid-stamp glyph instance after the run's glyphs (midpoint uv; key joins the touch pass), GPU goldens per kind + gate-term regressions + the § 4.5 dim-asymmetry pin (T8 flips it). Selection/caret = T7 seats. `[landed]`
- [Buiy text T7 — Selection + caret + placeholder painting](plans/2026-06-11-buiy-text-t7-selection-caret-placeholder.md) — the editing campaign's painting primitives: `CaretVisual`/`SelectionVisual` paint-input state (§ 6.3 pinned shapes; SelectionVisual = the normalized `(Cursor, Cursor)` endpoint pair) + the edge-only caret-blink render-prep writer in the Animate→Picking window (square wave of the virtual clock, `CaretBlinkInterval` 500 ms half-period, reduced-motion steady), `highlight()`-driven selection rects at quad seat 2 via `ExtractedTextQuads` (caller line-gate; upstream's empty-line + line-edge extensions; mixed-BiDi multi-rect inherited), per-cluster re-tint (`color.selection.fg`, atlas untouched), the caret as a § 3.3-snapped solid-stamp at seat 6 with the `caret-color` chain (explicit → `color.caret` if present → currentColor), `Highlight`/`HighlightText` forced-colors preference, value-compared carrier publish (blink re-uploads glyphs only — CPU half; GPU half = T8), `::placeholder` = one token through the unchanged pipeline. GPU: mixed-BiDi `::selection` golden + the caret-blink fixed-clock pair. `[landed]`
- [Buiy text T8 — Glyphs in effect groups + damage hardening](plans/2026-06-11-buiy-text-t8-glyphs-in-effect-groups.md) — the glyph buffer partitioned by effect-group ranges exactly like the quad path: per-entity `entity_runs` attribution on `ExtractedGlyphs` (one-tick lockstep publish) + fresh-node-list group derivation at prepare (the § 4.6 discipline; zero new producer params), the `RangePartitioner` hoist shared with the quad packer, CPU-only range recompute under the quad∪glyph union gate (blink still re-uploads glyphs only), the `Glyph@Rgba16Float` step-1 group draw (quads then glyphs, atlas `@group(1)` bound; the backgroundless-card empty-quad-range skip fixed) + flat complement draw (`TODO(text-seam)` resolved), the § 4.5 dim-asymmetry test flipped (everything in a group dims exactly once), the blink-reuploads-glyphs-only GPU pin via `BufferUploadStats`, the gate-#14 typing-latency mechanism fixture (headless; budget numbers stay with `buiy-verification-design`). `[landed]`
- [Buiy text T9 — Verification closure + docs flip](plans/2026-06-11-buiy-text-t9-verification-closure.md) — campaign closure: the gate-#2 golden-matrix expansion (`text_golden_suite_gpu.rs` — widget card, placeholder-vs-selected state pair, theme pair + swap-equals-cold, scale-factor-2.0 viewport; inline + double-capture discipline), the gate-#15 typing-churn pair (headless `text_typing_churn.rs` as the every-PR mechanism gate + the GPU pixels twin, ε = 0), the ASCII pre-warm **rejected** as unmeasured (architecture § 2.3 as-landed; re-open trigger in follow-ups.md), the T1–T7 spec errata consolidated into the section files, the cosmic-text/bevy-cosmic-edit prior-art correction pass (verification § 5 ledger), and the spec Status flip to implemented (rendering, T1–T9). `[landed]`

**Editing campaign**

- [Buiy text-editing campaign](plans/2026-06-13-buiy-text-editing-campaign.md) — E1–E6 phasing realizing `editing-and-ime.md`: editor substrate over `cosmic_text::Editor` → input/keymap → caret/selection → clipboard/undo → IME → lifecycle/widget/closure. Consumes T7's painting primitives; OQ#1 (edit→layout) resolved as one-frame latency; no new GPU work. `[landed]`
- [Buiy text-editing E1 — Editor substrate + Buffer ownership](plans/2026-06-13-buiy-text-editing-e1-substrate.md) — the editor substrate over `cosmic_text::Editor` and `TextBuffer` ownership handoff. `[landed]`
- [Buiy text-editing E2 — input translation + editing operations](plans/2026-06-13-buiy-text-editing-e2-input.md) — input translation, the editing-operation vocabulary, and the typing-latency gate. `[landed]`
- [Buiy text-editing E3 — caret + selection model + painting](plans/2026-06-13-buiy-text-editing-e3-caret-selection.md) — the caret + selection model and its painting via T7's primitives. `[landed]`
- [Buiy text-editing E4 — clipboard + undo/redo](plans/2026-06-13-buiy-text-editing-e4-clipboard-undo.md) — clipboard and undo/redo with composition-aware grouping. `[landed]`
- [Buiy text-editing E5 — IME composition](plans/2026-06-13-buiy-text-editing-e5-ime.md) — IME composition (preedit, candidate window, commit). `[landed]`
- [Buiy text-editing E6 — focus/lifecycle + widget + closure](plans/2026-06-13-buiy-text-editing-e6-lifecycle-widget-closure.md) — focus/lifecycle, placeholder, auto-scroll, the `TextInput` widget, and campaign closure. `[landed]`

### Authoring / BSN

Cross-cutting render + authoring: the Bevy 0.18→0.19-rc migration (the enabler), the `buiy_bsn` component-authoring layer (`bsn!` re-exports + widget scene-fns + component conformance), and the `buiy_view` **view-authoring** surface ("safer V" — `view(&Model) -> Element<Msg>` + a reconciler + typed routing over the MVU substrate, reached via `buiy::view`). The `.bsn` asset-file loader and component hot-reload stay deferred (await the upstream loader).

**Specs**

- [Buiy BSN integration design](specs/2026-06-18-buiy-bsn-integration-design.md) — Bevy 0.19-rc.3 pin (policy exception), render-graph→`Core2d` systems, `buiy_bsn` (`bsn!` re-exports + widget `#[require]` + scene-fns), reflect-gap closure; `.bsn` loader + hot-reload deferred. `[landed]`
- [Buiy view-authoring surface ("safer V") design](specs/2026-07-01-buiy-view-authoring-design.md) — the net-new `buiy_view` crate: the whole app-author surface is `Model` + `enum Msg` + `fn update` + `fn view(&Model) -> Element<Msg>`, realized by a library reconciler (positional + keyed, decomposed-style patch-in-place, reconcile-before-layout, drift-only controlled writes) + typed press/editor routers over the MVU funnel — deleting the hand-written `Changed<Model>` bind (DX-2) and `OnPress→Model` routing (DX-3). Typed tokens (`Space`/`Color`/`Radius`), `keyed_column`, `when`/`Empty`, `Element::map`, `ui(init,update,view)`, and the W4 steady-frame go/no-go gate. Reached via the `buiy::view` sub-prelude (distinct path — the `Element`-returning builders collide name-for-name with the `bsn!` scene-fns). The Phase-B FINAL of a prototype-first effort; PR1 scoped (Counter + TodoMVC), `Cmd::task`/capturing-`on_input`/`ControlledLeaf`/`ViewWidget` deferred to PR2/PR3. `[active]`

**Plans**

- [Bevy 0.19-rc + BSN migration](plans/2026-06-18-bevy-0.19-bsn-migration.md) — dependency-ordered phases (deps → compile → headless → GPU → `buiy_bsn` → `hello_bsn` → docs) realizing the BSN integration design. `[landed]`
- [Buiy view PR1 — audited-port execution plan](plans/2026-07-01-buiy-view-pr1.md) — the four-wave PR1 build (FW1 crate + Element + reconciler + press router + Counter → FW2 keyed lists + editor bridge + TodoMVC → FW3 conditional/`map` + work-counters + W4 gate → FW4 `buiy::view` prelude wiring + docs + full mechanical gate → merge-ready). An audited port of the throwaway safer-V prototype, re-implementing the deferred REFINE items (`#7`–`#12`, `#14`). Ships `crates/buiy_view` + `examples/counter_view` + `examples/todomvc_view`. `[active]`

### Widgets / interaction

The interactive widget-catalog subsystem — real `Pointer<E>` input, accessible stateful widgets, scroll/overlay/modal containers, F-tier styling, and a widget-gallery exemplar. Kicked off by re-implementing (clean-room, on current `main`) the throwaway TodoMVC prototype, grounded by its independent audit. Realizes the foundation's `interaction`/`media-and-widgets`/`accessibility`/`visuals` chapters. **Driven as a co-drive with the agent-interface (inspection-tools) campaign** — the tools are built alongside this app, demand-pulled, per the coordination plan (in Plans below). **Implementation COMPLETE + MERGED** — all six waves landed via PR #80, with the exact-parity gallery following in PR #83: the a11y substrate + the in-process inspection driver, all 8 APG widgets, scroll/overlay/menu/modal containers, F-tier styling + WCAG focus ring, and the C8 gallery (S1–S5); every interaction branch driven through the inspection driver.

**Specs**

- [Widget-catalog campaign design (umbrella)](specs/2026-06-22-buiy-widget-catalog-design/README.md) — charter for the subsystem: records the decisions (bevy_picking `Pointer<E>` input, full F-tier styling, gallery scope + the coordinate-space/text-integrity defaults) with rationale + rejected runner-ups; the realizes/supersedes map; the 9-child (C0–C8) decomposition + 6-wave sequencing + cross-cutting arbitration. **§2.7: coordinates with — does not duplicate — the landed/locked [agent-interface campaign](specs/2026-06-18-buiy-agent-interface-design/README.md)**, which owns the a11y substrate + APG widget bundles + action router + in-process driver; this campaign supplies the picking/coordinate/render/scroll/styling geometry those widgets need (incl. the stacking-aware `hit_test` the agent-interface campaign deferred) + the gallery. Builds on the incoming testing infrastructure (nextest + consolidated harnesses), never duplicating it. Child specs C1–C8 are in-folder. **Now driven as a co-drive** (see Plans below); the §8 gate is dissolved into the [coordination plan](plans/2026-06-22-widget-catalog-agent-interface-codrive.md). Implementation complete + merged (PR #80, then the parity gallery in PR #83). `[landed]`
- [C8-a S1 TodoMVC decision note](specs/2026-06-23-c8a-todomvc-s1-design.md) — `Kind: decision`. The gallery's S1 screen decisions (filter via the `A11yHidden` a11y-prune not a `Display` rewrite; the seed/clear channel = existing `SelectAll`+`Insert`; MultiClick edit-in-place) + rejected alternatives, for the C8 capstone. `[landed]`
- [Widget Catalog parity — FINAL design (Phase 3)](specs/2026-06-26-widget-catalog-parity-final-design.md) — the **mergeable** exact-parity target: the framework capabilities (dark theme + accent ramp, animation, Geist + letter-spacing, gradients, vector icons, backdrop-blur), the unified shell + 5 design-faithful screens + live accent theming, with the KEEP/REFINE/REDESIGN architecture (§2) re-decided from the prototype's full picture. Supersedes the prototype spec below. Merged in PR #83. `[landed]`
- [Widget Catalog parity — PROTOTYPE spec (Phase 2)](specs/2026-06-25-widget-catalog-parity-design-PROTOTYPE.md) — the exploratory prototype's target-state design (8-track research); proved exact parity is achievable end-to-end (never merged). Superseded by the FINAL design above. `[superseded]`
- [Widget Catalog exact-values reference](specs/2026-06-25-widget-catalog-values.md) — the single source of truth for the pixel reimplementation: every color token + the accent-ramp math, the box-shadow / radius / type / icon / gradient catalogs, and per-element layout, mined from the design HTML + its render JS. Implementers author against this, never the HTML. `[active]`
- [Gallery active/open-state reflect design](specs/2026-07-01-gallery-active-state-reflect-design.md) — `Kind: decision`. Two gallery controls whose active/open visual was applied only at spawn with no runtime reflect (Todo filter pills; overlay-menu ⋮ trigger) — the stepper-class no-op. Adds two change-detection reflect systems mirroring `reflect_rail_active_state`; rejects re-tinting in the press handler; surfaces the framework `Switch`-track + menu-item-highlight gaps as follow-ups. From the [widget-catalog audit](reports/2026-07-01-gallery-widget-catalog-audit.md). `[landed]`
- [Default `Switch` track recolor + `Menu` active-item focus ring](specs/2026-07-02-default-switch-track-and-menu-active-highlight-design.md) — `Kind: decision`. Resolves the two **framework-level** widget gaps the reflect design above deferred (audit N1/N2): `update_switch_visual` recolors the `SwitchTrack` fill (accent on / raised-alt off, white thumb — the default switch now matches the design like the showcase one), and `bind_menu_model` (inline) rings the roving-active `MenuItem` with a framework focus-ring `Outline` (a fill can't meet SC 1.4.11's 3:1 on the near-black menu panel; the ring is layout-neutral + clobber-free; folded into the bind rather than a sibling system so it adds no `Update` system — a separate system perturbed the MT-executor snapshot). Both fixed at the framework altitude with no golden churn. `[landed]`

**Plans**

- [Widget-catalog × agent-interface co-drive](plans/2026-06-22-widget-catalog-agent-interface-codrive.md) — the master cross-campaign sequencing & contracts plan: the unified dependency DAG, the **demand-pulled** tool-scope + deferral ledger (the inspection tools are built only as far as this app consumes them), the interleaved 6-wave plan, the four pinned shared contracts (SC-1…SC-4: OnPress sink / focus-visible / stacking-aware `hit_test` / scroll wire fields), and the grounding loops. Authoritative for sequencing/scope/contracts across both campaigns. `[landed]`
- [C1 — coordinate-space correctness](plans/2026-06-22-buiy-widget-catalog-c1-coordinate-space.md) — Wave-1 TDD plan: absolute basis via non-optional `GlobalTransform` + the `clip.rs` second-instance fix; un-ignores C7's offset-widget RED test. `[landed]`
- [C2 — editor text-integrity](plans/2026-06-22-buiy-widget-catalog-c2-text-integrity.md) — Wave-1 TDD plan: the Bug-2+3 single fix (`shape_stale` + the TextSync clobber), preedit-aware; un-ignores C7's font-reload-survival RED tests. `[landed]`
- [C7 — verification (real-input tier)](plans/2026-06-22-buiy-widget-catalog-c7-verification.md) — Wave-1 TDD plan: the `PointerHarness`, font-reload-survival harness, and content-presence invariant; RED-first, extends the landed nextest + consolidated test infra. `[landed]`
- [Widget Catalog parity — FINAL plan (Phase 3)](plans/2026-06-26-widget-catalog-parity-final.md) — the 6-wave "port the validated prototype + land the re-decided refinements" plan: API/prelude + extract-partition design, port A (theme/anim/fonts), port B (render caps) + the extract refactor, the shell + 5 screens, inspector + composite promotion + the headless plugin, and the Wave-6 parity audit + full gate + human-review prep. Merged in PR #83. `[landed]`
- [Gallery active/open-state reflect](plans/2026-07-01-gallery-active-state-reflect.md) — wave plan for the two reflect fixes (W1 filter-pill `reflect_active_filter` + false-comment fix; W2 menu-trigger `reflect_menu_button_open_state` + `MenuTriggerIcon`), each with a driven regression test; gate + verify + follow-up logging. `[landed]`

### State management (MVU)

MVU as Buiy's primary state interface in `buiy_core`: widgets route state changes through one ordered, recordable funnel, which buys record/replay, agent-drive, and the hot-reload foundation. Audit → prototype → production-final campaign.

**Specs**

- [MVU as core — FINAL design](specs/2026-06-29-mvu-as-core-design.md) — MVU as the primary state interface in `buiy_core`: the recordable message substrate (`Model`/`Cmd`/`Envelope`/`enqueue`/sealed `PureEnv`/`set_if_neq` single ordered drain + `MvuWorkCounters`), tiered granularity (router-leaf / stateful-leaf=drain-sole-writer / machine=Model+reducer / raw-ECS hatch), the early-window a11y-projection model (§4, corrected by the review gate), the AT synchronous act-then-observe seam (shared `fold_one_inline` + `InlineActionRegistry`), editor command-sourcing, the **scoped** record/replay guarantee (§7.3) + derived-structure-replay scope + debug write-outside-funnel auditor, the dismiss-through-the-funnel resource registry, single-writer completeness, and the **can-fail** L1 perf go/no-go gate. Re-decides placement to **core** on prototype-3's evidence; supersedes the unmerged 2026-06-26 opt-in-crate state-management draft. Realizes the [FINAL research synthesis](reports/2026-06-29-mvu-as-core-final-research/SYNTHESIS.md). Merged in PR #87. `[landed]`

**Plans**

- [MVU as core — FINAL implementation plan](plans/2026-06-29-mvu-as-core.md) — the wave-by-wave (W0 substrate+`set_if_neq` → leaf tier → editor command-sourcing → whole-UI record/replay → early-window menu machine + AT seam → dismiss un-invert → L1 perf gate → cleanup/docs/auditor) port-the-validated + land-the-re-decided plan realizing the design; one PR, merged in PR #87. `[landed]`

**Reports**

- [Demos → MVU migration — prototype journal + retrospective](reports/2026-06-30-demos-mvu-migration-journal.md) — app-author dogfooding of the shipped MVU substrate via a demo migration (counter / TodoMVC / gallery router). Substrate correct; DX gaps + recommendations for a *potential* core-ergonomics pass. `[active]`

**Prototype lineage & original research** (the prototype-first deliverables — the journal + retrospective ARE the prototype's product; preserved here so the learning survives worktree cleanup)

- [MVU-as-core — original research synthesis](reports/2026-06-26-mvu-as-core-research/SYNTHESIS.md) — the prototype-3 Stage-1 research (7 survey dossiers + synthesis, decisions D1–D10) that the FINAL [2026-06-29 research](reports/2026-06-29-mvu-as-core-final-research/SYNTHESIS.md) re-decided with the full picture. `[archived]`
- [Proto-3 charter](prototypes/2026-06-26-mvu-as-core-PROTO3-charter.md) · [design](prototypes/2026-06-26-mvu-as-core-PROTO3-design.md) · [journal](prototypes/2026-06-26-mvu-as-core-PROTO3-journal.md) · [retrospective](prototypes/2026-06-26-mvu-as-core-PROTO3-RETROSPECTIVE.md) — the MVU-as-core throwaway prototype's build-to-learn design, running journal (W1–W6), and the retrospective (keep/refine/redesign) that seeded the FINAL. `[archived]`
- Proto-1/2 — [proto-1 journal](prototypes/2026-06-26-elm-bevyified-state-journal.md) · [retrospective](prototypes/2026-06-26-elm-bevyified-state-RETROSPECTIVE.md) (bespoke runtime) and [proto-2 journal](prototypes/2026-06-26-elm-bevyified-state-PROTO2-journal.md) · [retrospective](prototypes/2026-06-26-elm-bevyified-state-PROTO2-RETROSPECTIVE.md) (Bevy-native), plus the [proto-2 wave-2 report](reports/2026-06-26-mvu-spike-wave2-report.md) — the earlier MVU prototypes that proved the substrate before proto-3. `[archived]`
- [State-management draft (opt-in crate)](specs/2026-06-26-buiy-state-management-design.md) — the original 2026-06-26 design; **superseded** by the FINAL core placement above, preserved as design lineage. `[superseded]`

### Performance

Cross-cutting per-frame performance: the audit → prototype → production-final campaign.

**Specs**

- [Performance final-pass design](specs/2026-06-26-buiy-performance-final-design.md) — the production design realizing the [audit](reports/2026-06-25-performance-audit.md): measurement infrastructure FIRST (deterministic hardware-INDEPENDENT gates — work-unit counters + dhat alloc + **iai-callgrind instruction counts**, the weak-machine backbone), then #9/#11/#5/#3/#16/#2 each red→green. Headlines the #5 atlas-touch blind-spot (`atlas_touch_ops` counter), #3's idempotent-by-construction layout gating, and #2's two-tier Full/Patch keyed partial re-extract (WebRender GpuCache + Bevy SyncWorldPlugin precedent). Constraints: 60 Hz hard floor / weak machines / zero new wasm obstacles. Measurement infra + 5 optimizations landed in PR #84. `[landed]`

**Plans**

- [Performance final-pass plan](plans/2026-06-26-buiy-performance-final.md) — phase-by-phase: Phase 0 (0a shared `bench_support` → 0b counters → 0c dhat+iai gates), Phase 1 (#9/#11/#5), Phase 2 (#3 gating), Phase 3 (#16 SoA), Phase 4 (#2 partial re-extract), Phase 5 (observability), then sustained autonomous iteration to the architectural wall. Includes the standing regression-guard (headless gate + GPU lane + live-interaction tier + gallery render) and the gallery S2 complex-scene workload. `[active]`
- [#2 keyed partial re-extract — implementation map](plans/2026-06-26-buiy-perf-2-implementation-map.md) — the current-code footprint-signature map staging audit #2 (the headline all-or-nothing extract cliff) onto HEAD; realizes the [final-pass design](specs/2026-06-26-buiy-performance-final-design.md) § #2. `[active]`

**Reports**

- [Multi-threading performance ceiling](reports/2026-07-02-mt-performance-ceiling.md) — does Buiy cap a MT-heavy app's performance ceiling? Measured (headless CPU-scheduler timing + the throwaway `examples/mt_ceiling` bench, 16-core host). Verdict: **yes but narrowly** — Buiy's per-frame work is a thread-invariant serial floor (never shrinks with cores), *tiny* for static/light UIs (sub-ms) but ~14 ms for an **active text-heavy** UI, capping a moderate-parallel app at **2.9×** worse (44 fps vs 128 fps @16 threads). Root cause pinned to ONE fixable spot: `extract_buiy_glyphs` is change-gated but **wholesale** ("§ 6.2 v1") — any change re-emits ALL glyphs (cost ∝ total text: 3.7/14/56 ms for 60/240/960 text nodes), unlike the node tier which got keyed-incremental re-extract in PR #84. Buiy's *update* logic is cheap+incremental and a good scheduling citizen (no barrier stalls). Fix = incremental glyph extract (a **perf-campaign** item), NOT threading Buiy (the MT executor taxes its serial path 1.15–1.22×). Extends the [MT-safety design](specs/2026-06-30-mt-safety-design.md) on the ceiling side it disclaimed. `[active]`

### Developer experience & LLM support

Cross-cutting authoring ergonomics with a first-class LLM-development workflow (author → compile → run headless → inspect the semantic tree → iterate). Prototype-first pass done; the FINAL landed track-by-track (B typed tokens, A probe, C one-surface, D corpus), each its own review-gated PR.

**Agent front door:** [`AGENTS.md`](../AGENTS.md) (repo root) — the one-import prelude, the widget/state/theme APIs, the GPU-free `buiy::probe` feedback loop, and the silent-wrong gotchas, for a coding agent authoring Buiy UIs. (Track D "buy corpus presence".)

**Specs**

- [First-class LLM development support design](specs/2026-07-01-first-class-llm-dev-support-design.md) — north-star framing the four tracks + the closed autonomous loop as one target: the loop/eyes (GPU-free semantic-tree probe, built on the Phase-0 in-process driver), kill-silent-wrong (typed **closed-enum tokens + theme contract**, missing-camera panic), one coherent surface (**Builder→Bundle** canonical constructor + domain accessors + typed events + curated prelude), buy-corpus-presence (accuracy-gated example pack + measured-priors naming). Re-weights the human-DX frictions F1–F8 through the LLM-as-author lens; validated by the [prototype retrospective](reports/2026-07-01-llm-dev-support-prototype-retrospective.md). Per-track status notes inline. (N1 "MVU not in prelude" was partly addressed upstream by #93.) `[active]`

**Plans**

- [Track B — typed theme tokens + contract](plans/2026-07-01-track-b-typed-tokens.md) — realizes design §3.2: closed-enum `ColorToken` (60 variants) + compiler-enforced `ThemeContract` + `PaletteMode{Normal,ForcedColors}`, killing the stringly-token silent-wrong class (the prototype's #1 deterministic failure). Merged in PR #113 (GPU 94/94 byte-identical). `[landed]`
- [Track A — the agent probe / "eyes"](plans/2026-07-02-track-a-agent-probe.md) — realizes design §4 Track A: `BuiyProbePlugin` (GPU-free no-render preset) + `snapshot_report` semantic-tree serializer + the `buiy::probe` front door + `examples/buiy_probe`, closing the author → run → inspect → drive loop with no GPU. `[active]`

### Reports

- [Developer-experience audit](reports/2026-06-25-developer-experience-audit.md) — what it looks like to *use* Buiy as an app author, grounded in `file:line` (8-way parallel read, hand-spot-checked). A guided tour of the three coexisting authoring idioms + a 6-theme friction inventory that defines the **F1–F8** frictions the interface redesign works from. Written pre-MVU (base `fdb8dda`); the friction picture is refreshed for the MVU base by the [current-state audit](reports/2026-06-30-current-state-audit-post-mvu.md). Fed the now-merged [`buiy_view` authoring surface](specs/2026-07-01-buiy-view-authoring-design.md) (PRs #106/#110/#111). `[active]`
- [UI developer-experience & composition prior-art research](reports/2026-06-25-ui-dx-composition-prior-art.md) — companion 14-agent research (mines the prior-art corpus + web-researches SwiftUI / Jetpack Compose / Flutter / Leptos+Solid signals / React+Radix / Rust API-ergonomics / Bevy BSN / typed design-token systems), every finding mapped to F1–F8 + rated for ECS+`bsn!` transferability. Ships a cross-cutting pattern vocabulary, a per-friction matrix, candidate design directions D1–D7, and prior-art promotion recs (→ the four [UI authoring & composition](prior-art/README.md) folders). **Written pre-MVU** — its state/event/reactivity directions (D1/D2/D6/D7) are now largely settled by MVU-as-core; treat the directions as *reconsiderable inputs*, not conclusions. `[active]`
- [Current-state audit (post-MVU-as-core)](reports/2026-06-30-current-state-audit-post-mvu.md) — the interface-redesign **resumption baseline**: current-state audit of `main` @ `abb76fb` after PR #87 (MVU-as-core), 7-agent grounded read, `file:line`-cited, headline claims hand-verified. Verdict: MVU shipped as a clean **tiered widget-internal** substrate but was app-author-invisible (zero MVU in the gallery, nothing in `buiy::prelude`); it settled F1's architecture at the machine tier, relocated it at the leaf tier, and left F2/F4/F5/F6/F7/F8 open. Ships the prioritized backlog the [`buiy_view` restart](specs/2026-07-01-buiy-view-authoring-design.md) worked from. `[active]`
- [Gallery widget-catalog audit](reports/2026-07-01-gallery-widget-catalog-audit.md) — "test every widget" QA pass over the 5-screen gallery (baseline → force-state GPU render of every widget state → driven behavioral no-op sweep → adversarial verification). Found + fixed two gallery-local no-ops (filter-pill highlight; menu-trigger open-state) via [the reflect design](specs/2026-07-01-gallery-active-state-reflect-design.md); surfaced two framework gaps (default `Switch` track, menu-item highlight) as follow-ups; verified the rest correct. Those two follow-ups were then resolved at the framework altitude in the [switch-track + menu-ring decision](specs/2026-07-02-default-switch-track-and-menu-active-highlight-design.md). `[landed]`

- [Performance audit](reports/2026-06-25-performance-audit.md) — first performance-lens pass (19-agent subsystem + prior-art workflow, one real benchmark, perf-validated critic; 108 findings → 16 prioritized). Headline: Buiy is **measurement-blind**; the one measured number — a static 128-node text screen at 10.9 ms/frame — root-causes to the O(V·E) atlas-LRU touch (#5). Two bigger suspected cliffs: all-or-nothing extract→prepare rebuild (#2) + ~12 ungated full-tree idle passes (#3). Ships a 6-phase measurement-infra roadmap ending in a non-flaky CI gate. Realized by the [final-pass design](specs/2026-06-26-buiy-performance-final-design.md); **findings #1/#5/#9/#3/#11/#2 resolved** in the [final results](reports/2026-06-26-buiy-performance-final-results.md) (PR #84). `[landed]`

- [Performance final — results](reports/2026-06-26-buiy-performance-final-results.md) — what the campaign landed (20+ gate-proven commits on `worktree-perf-final`): Phase-0 measurement infra (work-unit counters incl. the `atlas_touch_ops` #5 blind-spot closer, dhat gate, shared bench harness) + 5 optimizations — **#5 atlas O(1) 8.6×**, #9 dead-path delete, **#3 idle layout −22/−32 %**, #11 EntityHashMap, and the headline **#2 keyed partial re-extract (8 verified stages A→D2)**: an interactive change went from rebuild+re-upload ALL N node instances to **re-resolve 1 + re-upload 1**, proven pixel-identical on a real GPU (`render_patch_upload_gpu.rs` + the work-counter flip + 92 goldens + gallery). Documents the v1 Patch scope (pure-bg-quad group-free value changes) + the remaining work: #16 SoA now marginal, D3 (band/shadow/gradient partial) optional, and **#13b (opaque/depth pre-pass) flagged as the architectural wall** (a render-graph change needing a human decision); #13a (viewport-AABB cull) named as the sensible next campaign. Landed in PR #84. `[landed]`

- [Text-editing design-readiness review](reports/2026-06-13-text-editing-design-readiness.md) — three-verifier audit of `editing-and-ime.md` against current `main` before the `buiy-text-editing` campaign: every integration seam confirmed in code, the no-new-GPU-work painting claim upheld, and OQ#1 (edit→layout frame-ordering) resolved as accepted one-frame latency. Verdict: ready-with-patches (now applied). Its readiness premise is overtaken: the campaign it gated landed as E1–E6 (PRs #62–#67). `[superseded]`
- [Visual-bug detection strategy](reports/2026-06-14-visual-bug-detection-strategy.md) — how to catch visual regressions as Buiy scales: a five-tier pyramid (layout-number → display-list/paint-order → metamorphic/property → reftests + CPU-cross-check → golden screenshots), reftests-first, grounded on canonical `main`. Audits the existing golden/forced-colors/text-shaping infra and names the gaps; input to `buiy-verification-design`. Pairs with the five `prior-art/` folders below. Realized by the [verification design](specs/2026-06-15-buiy-verification-design/README.md) + the landed `buiy_verify` harness. `[superseded]`
- [Verification-harness adversarial review](reports/2026-06-15-verification-harness-adversarial-review.md) — fresh-agent cold-context review of the landed `buiy_verify` harness (20-agent find → verify → synthesize workflow). Found + **fixed** 7 real bugs (2 high: a blank render silently passing any golden; `GoldenKey` dropping the forced-colors axis), 1 maintainability trap, and the recurring "docstrings oversell coverage" theme; recorded 3 coverage gaps as follow-ups. Both gates green. `[landed]`
- [Bevy 0.19-rc + BSN migration research](reports/2026-06-18-bevy-0.19-bsn-migration-research.md) — file:line inventory of the Bevy 0.18.1→0.19.0-rc.3 bump (render graph removed → `Core2d` systems, wgpu 27→29, accesskit, ECS/reflect) and the `bsn!` authoring surface, mapped onto `buiy_core`. Input to the BSN integration spec/plan; realized by the landed bump (PR #70). `[landed]`
- [Documentation audit](reports/2026-06-18-docs-audit.md) — full accuracy/quality/spec-alignment pass over all of `docs/` (26-agent workflow + adversarial code-verification): 110 findings, ~50 distinct issues. Headline: docs lag the code, zero code defects; dominant failure is stale "deferred/draft" tags left in place after work shipped. `[landed]`
- [Documentation coverage & quality audit](reports/2026-06-30-documentation-coverage-audit.md) — 6-agent coverage/quality pass over README + docs/ + rustdoc + examples + skills @ `d285e6b`: design docs are deep and (post docs-sync) accurate, but **usage/how-to docs are the systemic gap** — no getting-started guide, no worked MVU example, a dead `--test goldens` bless command, the Style builder 64/84 methods undocumented, no CONTRIBUTING; cheap accuracy defects fixed in-pass, a 9-item remediation backlog remains. `[active]`
- [Code-side findings from the docs audit](reports/2026-06-18-spec-code-findings.md) — the code-adjacent observations split out of the docs audit (stale in-code comments, dead-legacy `extract_buiy_draws` path, spec targets not yet built); no functional code defects found. `[active]`
- [Testing infrastructure & coverage audit](reports/2026-06-18-testing-infrastructure-coverage-audit.md) — whole-estate audit of the test suite + CI + harness + determinism + build ergonomics (15-dimension fan-out → adversarial per-finding verification → completeness critic, 64 agents; 79/80 findings survived). Verdict: infra strong & disciplined for the project's age, suite green; 9 high-severity exposures (gitignored `Cargo.lock`; flex justify/align + `coverage.wgsl`/`composite.wgsl` verified only in the GPU `#[ignore]` lane; picking z-order + focus `tab_order` branches dead; Tier-3 invariant re-implements paint 6f; cross-rasterizer golden contradiction; orphaned facade GPU test in no lane; ~162-binary link explosion) plus 5 wholly-absent dimensions (runnable doctests, perf, coverage tooling, `hello_text`/`capture` e2e, CI robustness guards). Builds on the 2026-06-15 harness review; ships a 33-item backlog. **✅ RESOLVED 2026-06-19** — all 43 findings + 2 cleanups landed via the [resolution campaign](plans/2026-06-18-testing-audit-resolution-campaign.md). `[landed]`
- [Agent ↔ running-app interaction surface](reports/2026-06-18-agent-interaction-surface-research.md) — research input to a future agent-interface spec: how LLM agents could drive UI transitions + inspect a running Buiy app. Key finding — Buiy already *authors* an AccessKit semantic tree (the same surface Playwright-MCP/Flutter/Compose use), so it is better-positioned than a browser; the one gap is making it bidirectional (consume `ActionRequest`s via the existing `bevy_winit` channel). Recommends semantic-tree-first, MCP-primary/BRP-debug-tier, two-tier transport. Names 6 prior-art folders to spawn. `[active]`
- [TodoMVC → production instruction set](reports/2026-06-18-todomvc-to-production-instruction-set.md) — the throwaway TodoMVC prototype's own handoff/self-report (PR #73): 3 surfaced `buiy_core` bugs (picking relative-coords, text-commit async-font crash, unfixed TextSync editor-clobber), the A11yToggled/Checkbox/keyboard additions, widget/styling/verification gaps, and an A→F work breakdown. Ad-hoc and systematically optimistic — read alongside the independent audit below, which corrects and extends it. `[superseded]`
- [TodoMVC prototype audit](reports/2026-06-21-todomvc-prototype-audit.md) — independent 17-agent audit of the prototype + its handoff doc against current `main`: confirms all 3 bugs are real and live, finds the picking bug is a coordinate-space **class** bug (`clip.rs` is a second instance), that the text-commit guard would **mask** the unfixed editor-content-clobber (data loss), and that keyboard **focus is structurally invisible** (WCAG 2.4.7). Distills 3 Tier-0 architecture decisions + a 23-item, alternative-flagged work-list. Grounds the re-implementation spec/plan. `[active]`
- [Widget-catalog live-run crash + invisible-content bug](reports/2026-06-24-widget-catalog-rendering-and-crash-bugs.md) — running `buiy_gallery` for the first time exposed two bugs the 1653-green headless gate missed. **(1)** A live crash: editor edits in `BuiySet::Input` un-shape the buffer *after* `TextCommit`, so an edit coinciding with any other damage reached the render extract unshaped — the headless gate never ran the render world. Fixed by `reshape_edited_editors` (same-frame editor reshape) + a debug-only `Last`-scheduled coherence invariant (the headless-reachable mirror of the extract assert). **(2)** Invisible content: `Text` lacked `#[require(Node)]`, so scene-authored bare-`Text` labels never laid out or painted (the layout snapshots had *baked in* their absence); buttons had no visible label child. Fixed by `Text` `#[require(Node)]` + restructuring the labelled controls to flex-rows `[control-box, label]` + content-width buttons with centered labels. New guards: a bare-`Text`-lays-out invariant + a gallery content-paints test. `[active]`
- [Widget Catalog parity — FINAL DECISIONS (reviewer's guide)](reports/2026-06-26-parity-final-decisions.md) — the human-review gate's decision record for the `parity-final` branch: the prototype→final narrative, the KEEP/REFINE/REDESIGN architecture as it actually shipped (the `NodePaintQuery` projection + its spec-premise correction, `BuiyHeadlessPlugin`, the composite + prelude promotions, `AnimatedBackgroundColor` auto-composite, the LetterSpacing-px + light-default binding overrides), the verification evidence (1791 headless + 69+22 GPU + the 6 screenshots), the resolved M1–M6 framework bugs, and the residual gaps + follow-ups. `[active]`
- [Widget Catalog parity — FINAL research (re-decided architecture)](reports/2026-06-26-parity-final-research-decisions.md) — the 7-track fleet's re-evaluation of every prototype decision with the full picture: per-subsystem KEEP/REFINE/REDESIGN verdicts + build strategy, synthesized into the FINAL spec + plan. `[active]`
- [Gallery interactivity root-cause + live-interaction test tier](reports/2026-06-26-gallery-interactivity-rootcause.md) — `cargo run -p buiy_gallery` rendered all 5 screens but was **totally non-interactive**: `emit_picks` hit-tested every node with a layout box, ignoring visibility, so the detached, full-window, top-layer **modal `Dialog`** (`CssVisibility::Hidden`, never `Display::None`d by the router, present on every screen) absorbed every click while painting nothing → `OnPress` never fired. Fix: `emit_picks` skips any `ComputedPaintSkip` node — **pick-set == paint-set** (the visibility analogue of the pick-order==paint-order co-drive). The whole prior suite missed it because no test composed the **real shell** and drove a **real pointer click** through the **real backend** (state-injection / a11y-driver / minimal-app / static-snapshot only). Closes the gap with a live-interaction tier: 16 shell-integration tests (`examples/buiy_gallery/tests/interaction.rs`) + 1 backend regression (`crates/buiy_core/tests/crosscut/picking_backend.rs`). Faithfulness proven: reverting the fix fails 15/16 Tier-A + Tier-B. Sibling of the [2026-06-24 live-run report](reports/2026-06-24-widget-catalog-rendering-and-crash-bugs.md). `[active]`
- [Widget Catalog parity — prototype research findings](reports/2026-06-25-parity-research-findings.md) — the prototype phase's 8-track capability research (theming/tokens, gradients, animation, backdrop-blur, icons/vector, fonts/letter-spacing, shell+router+composites, dotted-grid): the build-designs + weighed alternatives that seeded the prototype spec. `[active]`
- [WASM / browser-rendering feasibility](reports/2026-06-25-wasm-browser-support-feasibility.md) — what it would take to render Buiy in the browser (23-agent workflow → coordinator re-anchored every fact on `fdb8dda`). Verdict: **feasible, moderate effort, no architectural rewrite** — the renderer is in the WebGL2 downlevel subset by construction (no compute/storage/MRT/depth-stencil; format/sample-specialized pipelines; fragment-discard clipping; embedded font; clipboard facade; single-threaded). Two hard fixes: `arboard` is an ungated dep with no wasm backend (compile blocker), and the border/outline band pipeline declares 17 vertex attributes (`@location 0..16`) exceeding WebGL2's 16 cap (render blocker). Strategy: WebGPU-first MVP (renderer unchanged), WebGL2 fallback build for reach (+`Rgba16Float` fallback). Web a11y reaches no screen reader (null adapter) — the one large, optional piece. `[active]`
- [Browser-reach widening — prototype retrospective](reports/2026-06-30-browser-reach-widening-prototype-retrospective.md) — the throwaway prototype (built + RAN Buiy on WebGL2 in a real browser) that seeds the [widening design](specs/2026-06-30-buiy-browser-reach-widening-design.md). Verdict: **achievable + proven by running** — full gallery renders + interacts on WebGL2 (zero shader errors; a small 7-crate `Cargo.lock` delta, cargo-deny clean) after one band-attribute fold. Keep/refine/redesign per decision; key run-only learnings: touch's Part A (location-sync) is **necessary but NOT sufficient** (needs press-based activation too), `Rgba16Float`'s float-less break is not reproducible on desktop/SwiftShader (both expose the extensions), and `accesskit_web` **does not exist** (a11y → DOM/ARIA overlay is the only path). `[active]`
- [Codebase state & spec-gap audit](reports/2026-06-25-codebase-state-and-spec-gap-audit.md) — whole-codebase audit of `origin/main @ fdb8dda` + the in-progress Widget-Catalog parity prototype/FINAL (13-agent fan-out + a completeness critic). Built subsystems (layout/render/text/editing/verify/a11y/bsn) track their specs faithfully with real two-lane coverage; the **big gaps are the negative space** — unbuilt foundation areas (images/media, ~18 missing F-tier widgets, the event model, gamepad, drag-drop, the animation surface, OS-pref binding) several of which are **untracked**, agent-interface P2/`buiy_mcp` = 0%, thin verification breadth (2+2 golden/reftest cells), and a systemic wholesale-rebuild scaling pattern. Ground truth: local `main` was 72 commits stale; agent-interface P0–P1d are landed; the parity FINAL is not yet merge-ready (its Wave-4 shell/screens landed mid-audit in `b881008`; Waves 5–6 + the review-gated PR remain). `[active]`
- [MVU-as-core — FINAL research synthesis](reports/2026-06-29-mvu-as-core-final-research/SYNTHESIS.md) — the Stage-1 research behind the [FINAL MVU design](specs/2026-06-29-mvu-as-core-design.md), re-deciding every prototype-3 decision with the full picture (seeded by the prototype-3 retrospective + audit). Five research dossiers — RD1 (AT act-then-observe seam), RD2 (the can-fail L1 perf gate), RD3 (record/replay scope), RD4 (the port map), RD5 (the migration ledger) — synthesized into the design's D1–D14 decision log. `[active]`
- [Hot-reload feasibility](reports/2026-06-26-hot-reload-feasibility-research.md) — research input to a future `buiy-asset-pipeline-design` / hot-reload spec: decomposes hot-reload into five dimensions (asset / style-theme / scene-`bsn` / Rust-code / state-preservation), audits Buiy's existing surface (the font-reload beachhead, the deferred `.bsn` loader, the un-reflected `TextEditState`/`TextBuffer` state crux) cited to file:line, surveys the external prior art (Flutter/React/SwiftUI stateful reload, Bevy `hotpatching`/subsecond, Live++/Unreal/Unity live-coding, Slint/Makepad/bevy_flair DSL reload, Erlang `code_change`/CLOS/Elm migration canon), and recommends a layered path. Names 4 new + 7 extend prior-art folders. The *research* stage; measured + superseded by the experiment findings below. `[active]`
- [Hot-reload experiment findings + verified solutions](reports/2026-06-26-hot-reload-experiment-findings.md) — the *experiment* stage that **measures** and supersedes the feasibility matrix: a throwaway `hot_reload_lab` prototype (built prototype-first, **code unmerged** on `worktree-hot-reload-prototype`) RAN probes across all five dimensions and **attacked every problem found**. The decisive variable is **whether the reload respawns entities** — in-place reloads (asset/font, theme) preserve the whole live state by construction; respawn reloads (scene re-apply, full-tree) destroy it. **Keystone:** Buiy's MVU `LogicalId`+`Reflect` replay path, reused *verbatim*, IS the state-preserving-reload answer (modeled value survived a respawn; un-modeled editor state died). **Every state-and-scene problem is solvable today without a `buiy_core` change** (verified solution probes: keyed reconciler + description-vs-runtime ownership split fixes scene duplicate/leak/clobber; editor `{text,caret,selection}` carve-out; `LogicalId`→`Entity` focus remap). The lone true blocker is the upstream `.bsn` text loader; live code-patch + visual repaint are env/verification gaps. Converges with the MVU-as-core direction — co-design recommended. `[active]`
- Hot-reload experiment prototype lineage — [journal](prototypes/2026-06-26-hot-reload-experiment-journal.md) · [retrospective](prototypes/2026-06-26-hot-reload-experiment-RETROSPECTIVE.md) — the prototype-first deliverables behind the experiment-findings report above (the `Prototype` doc type): the wave-by-wave journal (7 characterization + 3 solution probes, each RUN + verified) and the keep/refine/redesign retrospective that seeds the FINAL hot-reload spec. Prototype CODE stays unmerged on `worktree-hot-reload-prototype`. `[active]`
- [Demos → MVU migration — prototype dev journal + retrospective](reports/2026-06-30-demos-mvu-migration-journal.md) — dogfooding the shipped `buiy_core::mvu` as an **app author** by migrating the demos, prototype-first: a **counter** (`hello_button`), a **TodoMVC** (`examples/todomvc`), and the **gallery screen router** (strangler migration via `fold_one_inline`) — each built, run on a real GPU, screenshot-verified, and covered by green logic tests (counter 3/3, todo 5/5, gallery 29/29, zero regression). Verdict: **the migration works and the substrate has ZERO correctness bugs**; the cost is DX/ergonomic gaps + one empirically-confirmed known limitation. Headline findings for a *potential* core-ergonomics pass: (1) the `buiy` prelude exposes **no** MVU types (the first wall); (2) the missing **declarative View** — a `Vec<Todo>` reconcile is ~60 hand-written lines vs ~5 in Elm/Iced; (3) no `OnPress→Model` **routing** helper; (4) app-global state must be a component, not a Resource; (5) **structural/keyed-list replay is unproven** — the live TodoMVC's add/remove are off-log; (6) leaf-widget state vs app-model has no composition story; (7) `fold_one_inline` is the *positive* find — it makes incremental migration of an existing applier tractable. Ends with a value÷risk-ranked recommendation list. `[active]`
- [LLM development support research + LLM-lens audit](reports/2026-07-01-llm-development-support-research.md) — what "first-class support for LLM development" means for Buiy's authoring interface (23-agent verified workflow): principles P1–P5, the F1–F8 + MVU N-frictions re-ranked by LLM-as-author impact (silent-wrong/stringly-tokens rise to critical because a sightless agent's only loop is the compiler; raw verbosity drops), and an evidence ledger with adversarial-verification calibration (CONFIRMED: training-data-presence dominance, ~94% of compiler-catchable LLM errors are type errors; several attractive numbers downgraded to overstated/refuted). Companion to the 2026-06-25/30 human-DX corpus; feeds the [design](specs/2026-07-01-first-class-llm-dev-support-design.md). `[active]`
- [LLM-dev-support prototype journal](reports/2026-07-01-llm-dev-support-prototype-journal.md) — prototype-first dev journal (code unmerged): built + RAN a GPU-free semantic-tree eval harness (`examples/llm_probe`) and measured real agents authoring Buiy UIs. E1 (capable agent + source = high static baseline → the loop's value is the runtime/silent-wrong class), E3/E3b keystone (a prelude-only agent hallucinates the prior-matching API the research prescribes; the compiler loop converges then STALLS at non-prelude symbols / stringly tokens), N=4 broadened probes, and the R1/R2/R3 feasibility spikes (runtime loop catches compile-green bugs; typed tokens+contract; Builder→Bundle children-via-observer) — all proven first-hand. `[active]`
- [LLM-dev-support prototype retrospective](reports/2026-07-01-llm-dev-support-prototype-retrospective.md) — the gate output: verdict + keep/refine/redesign per track, framework facts surfaced by running it, the N=4 broadened validation + the R1/R2/R3 spikes, and the re-decided FINAL build order (Track B typed tokens first → Track A probe/eval-rig → C → D), each validated by the measured-priors probe. Seeds the [design](specs/2026-07-01-first-class-llm-dev-support-design.md). `[active]`
### Docs infrastructure

**Specs**

- [Docs organization design](specs/2026-05-07-docs-organization-design.md) — target structure of `docs/`, naming, headers, nesting. `[landed]`

**Plans**

- [Documentation audit remediation](plans/2026-06-18-docs-audit-remediation.md) — ordered batches that resolved the [documentation audit](reports/2026-06-18-docs-audit.md) findings (catalog gaps, stale statuses, misleading "deferred" claims, convention drift) and restored conformance to the docs-organization spec. Executed 2026-06-18. `[landed]`

## Prior art

**The full prior-art catalog lives in [`prior-art/README.md`](prior-art/README.md)** — 48 external systems Buiy learns from, one folder per system. It was split out of this master index: at 48 rich entries it had grown to ~half the file and stopped being scannable, so the master keeps only the category map below. Living documents, driven by the `researching-prior-art` (creation) and `using-prior-art` (consumption) skills.

- **Bevy UI ecosystem** — bevy_ui, bevy_ui_widgets, bevy_picking, bevy_feathers, bevy_a11y, bevy_flair, belly, bevy_egui, bevy_lunex, woodpecker_ui.
- **Non-Bevy Rust GUI** — Freya, Xilem/Masonry, GPUI, Makepad, Floem, Dioxus, egui, Iced, Slint.
- **State management & reactivity** — the reactive/MVU precedents (consumed by the [State management (MVU)](#state-management-mvu) area above).
- **Game engine UI systems** — NoesisGUI, Unreal Slate+UMG, Unity UI, Godot Control, RmlUi, Coherent Gameface.
- **Web / browser engines** — Servo/Stylo, Blink (the reference CSS implementations Buiy mirrors).
- **Agent interface** — MCP/LLM-UI control, browser automation, Flutter/Compose semantics, engine devtools protocols, Bevy Remote Protocol.
- **Substrate primitives** — Taffy, cosmic-text, AccessKit, WAI-ARIA APG, web-rendering.
- **Verification & visual testing** — wpt-reftests, vello, skia-gold, flutter-golden-testing, wgpu-testing.
- **Archived** — no longer tracked: bevy_cosmic_edit, kayak_ui, sickle_ui.

## Reference designs

Archived design bundles — immutable inputs to specs, not specs themselves. Each lives in its own `reference-designs/<name>/` folder (no date prefix).

- [Widget Catalog design bundle](reference-designs/widget-catalog/) — the source design HTML (`Widget Catalog.dc.html`) the Widget-Catalog parity work was reimplemented against pixel-for-pixel. The exact-values table [`specs/2026-06-25-widget-catalog-values.md`](specs/2026-06-25-widget-catalog-values.md) is mined from it; realized by the [parity FINAL design](specs/2026-06-26-widget-catalog-parity-final-design.md).

## Conventions

Cemented in [`specs/2026-05-07-docs-organization-design.md`](specs/2026-05-07-docs-organization-design.md). Mirrored on demand by the `organizing-buiy-docs` skill. Summary:

### Naming

| Type | Pattern | Example |
|---|---|---|
| Spec | `specs/YYYY-MM-DD-<kebab>-design.md` | `2026-05-07-docs-organization-design.md` |
| Multi-file spec | `specs/YYYY-MM-DD-<kebab>/README.md` + children | `2026-05-07-example-design/README.md` |
| Plan | `plans/YYYY-MM-DD-<kebab>.md` | `2026-05-07-example-plan.md` |
| Report | `reports/YYYY-MM-DD-<kebab>.md` | `2026-05-07-example-audit.md` |
| Prior-art | `prior-art/<system>/README.md` (no date prefix) | `prior-art/bevy-feathers/README.md` |
| Prior-art child | `prior-art/<system>/<facet>.md` | `prior-art/bevy-feathers/architecture.md` |

The date is when the doc was written, not the implementation target. The `-design.md` suffix on specs is what visually distinguishes specs from plans in `ls` output. Prior-art uses the system name (no date prefix) because the folders are living docs — track revision via git, not filename.

### Document headers

Every new spec, plan, and report opens with:

```
**Date:** YYYY-MM-DD
**Status:** draft | active | landed | superseded
**Kind:** campaign | prototype | final | decision | values | ...   (optional — only when the plain type is ambiguous)
**Spec:** specs/...      (plans only — REQUIRED, points at the spec being realized)
**Supersedes:** specs/... (if applicable)
```

Prior-art docs use a different header:

```
**Date:** YYYY-MM-DD       (last meaningful update — bump on revision)
**Status:** active | archived
**Subject:** <System name + one-line scope>
```

### Nested folders

Use a folder (`specs/YYYY-MM-DD-<topic>/README.md` + children) only when one logical document is too large for a single file *and* the children are tightly coupled. Children use kebab-case topic names (no date prefix — they inherit the parent's date). Maximum one level deep. Multiple independent docs sharing a topic stay flat.

### Adding a new spec, plan, or report

1. Pick the right type (spec = target, plan = migration, report = audit, prototype = a prototype's carried-over journal + retrospective, prior-art = external-system deep-dive).
2. Name with `YYYY-MM-DD-<kebab>-design.md` (spec) or `YYYY-MM-DD-<kebab>.md` (plan/report).
3. Add a one-line entry to this README under the right area, with a 5–15 word summary and `[draft]` tag.
4. Plans must include `**Spec:** specs/...` in their header.
5. Multi-file specs nest under `YYYY-MM-DD-<topic>/` with a required `README.md`.
6. **When work lands, reconcile** (at branch-finish): catalog every new doc (required — missing entries are not tolerable) and flip realized specs/plans to `landed`/`superseded` in both the header and the `[tag]` (expected hygiene). See the design spec's *Reconciling when work lands*.
