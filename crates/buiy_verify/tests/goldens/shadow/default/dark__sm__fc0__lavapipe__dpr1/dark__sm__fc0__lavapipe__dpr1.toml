[key]
widget = "shadow"
state = "default"
theme = "dark"
viewport = "sm"
forced_colors = false
backend = "Lavapipe"
dpr = 1000

[[positives]]
file = "dark__sm__fc0__lavapipe__dpr1.0.png"
blessed_commit = "96f2ea27d45730be5eed2d51365830256369e992"
blessed_at = "2026-07-02T01:29:27Z"
reason = "Δ1 widen for known lavapipe shadow-blur jitter: the pinned software rasterizer (Mesa 24.3.4) produces a runner-dependent FP-ordering difference of ±1 LSB on the drop-shadow blur across CI hosts (observed on PR #110's GPU lane: differing_pixels=0, max_channel_delta=1; the same commit passed on rerun and renders delta=0 on a local pinned-lavapipe). Only the RAW per-channel gate is relaxed to 1 LSB; max_diff_pixels stays 0 so the perceptual gate is untouched — a genuine regression (≥2 LSB, or any perceptually-differing pixel) still fails."

[positives.budget]
max_channel_delta = 1
max_diff_pixels = 0
